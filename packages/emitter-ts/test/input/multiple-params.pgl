database {
  create table users (
    id integer not null,
    name text not null,
    age integer not null
  );
}

query search(name: text, min_age: integer) {
  select id, name, age
  from users
  where name = $name AND age > $min_age
}
