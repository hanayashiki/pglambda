# Antlr Wrapper

This package mainly includes ANTLR syntax in source folder and generated TypeScript.

ANTLR-generated TypeScript requires special tsconfig which is not modern enough for other parts, so we repackage the generated code.

## Build the Package

```bash
pnpm build
```
